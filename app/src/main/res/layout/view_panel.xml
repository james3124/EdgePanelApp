<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:cardBackgroundColor="#E6121212"
    app:cardCornerRadius="20dp"
    app:cardElevation="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Top Bar: Title + Fullscreen + Close -->
        <LinearLayout
            android:id="@+id/panel_title_bar"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingStart="12dp"
            android:paddingEnd="8dp"
            android:background="#1A1A2E">

            <!-- Drag hint dots -->
            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="⠿ Edge Panel"
                android:textColor="#CCCCCC"
                android:textSize="13sp"/>

            <!-- Fullscreen button -->
            <TextView
                android:id="@+id/btn_fullscreen"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:text="⛶"
                android:textSize="18sp"
                android:textColor="#AAAAAA"
                android:gravity="center"
                android:background="@drawable/circle_button_bg"
                android:layout_marginEnd="6dp"/>

            <!-- Close button -->
            <TextView
                android:id="@+id/btn_close"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:text="✕"
                android:textSize="16sp"
                android:textColor="#FF5252"
                android:gravity="center"
                android:background="@drawable/circle_button_bg"/>
        </LinearLayout>

        <!-- Tab Bar -->
        <HorizontalScrollView
            android:layout_width="match_parent"
            android:layout_height="44dp"
            android:scrollbars="none"
            android:background="#161625">

            <LinearLayout
                android:id="@+id/tab_layout"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"/>
        </HorizontalScrollView>

        <!-- Panel Content -->
        <FrameLayout
            android:id="@+id/panel_content"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1">

            <FrameLayout
                android:id="@+id/panel_content_container"
                android:layout_width="match_parent"
                android:layout_height="match_parent"/>
        </FrameLayout>

        <!-- Bottom Bar: Min/Max button -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="44dp"
            android:orientation="horizontal"
            android:gravity="center"
            android:background="#1A1A2E">

            <TextView
                android:id="@+id/btn_min_max"
                android:layout_width="72dp"
                android:layout_height="32dp"
                android:text="⬇"
                android:textSize="18sp"
                android:textColor="#BBBBBB"
                android:gravity="center"
                android:background="@drawable/pill_button_bg"/>
        </LinearLayout>

    </LinearLayout>
</androidx.cardview.widget.CardView>
